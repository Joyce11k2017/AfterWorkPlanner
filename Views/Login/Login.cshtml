@model AfterWorkPlanner.Models.User

@{
    ViewBag.Title = "Login";
}

<h2>Login</h2>

@using (Html.BeginForm("Login", "Login", FormMethod.Post))
{
    <fieldset>
        <legend>Plan Your After Work Activity</legend>

        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)
        @if (@ViewBag.Message != null)
        {
            <div style="border: 1px solid red">
                @ViewBag.Message
            </div>
        }
        <table>
            <tr>
                <td>@Html.LabelFor(a => a.user_name, "User Name")</td>
                <td>@Html.TextBoxFor(a => a.user_name)</td>
                <td>@Html.ValidationMessageFor(a => a.user_name)</td>
            </tr>
            <tr>
                <td>
                    @Html.LabelFor(a => a.user_pwd, "Password")
                </td>
                <td>
                    @Html.PasswordFor(a => a.user_pwd)
                </td>
                <td>
                    @Html.ValidationMessageFor(a => a.user_pwd)
                </td>
            </tr>
            <tr>
                <td></td>
                <td>
                    <input type="submit" value="Login" />
                </td>
                <td></td>
            </tr>
        </table>
    </fieldset>
}

